import React from 'react';
import PropTypes from 'prop-types';

const Swipeable = props => {
  //  This is to prevent eslint from errors

  return;
  //  This is to prevent eslint from errors
};

export default Swipeable;

Swipeable.propTypes = {
  children: PropTypes.element.isRequired,
};
